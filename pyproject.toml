[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Ultorex api document"
authors = [{name = "ultorex", email = "support@ultorex.io"}]
dynamic = ["version", "description"]
